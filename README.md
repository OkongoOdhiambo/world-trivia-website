# World Trivia — Accessible, Responsive Web Page

Brief: A single-page, accessible, mobile-first trivia site skeleton. Semantic HTML, robust ARIA, keyboard support, high-contrast palette and responsive layout.

## File structure
- index.html
- css/styles.css
- README.md

## Usage
1. Place files in project root.
2. Open `index.html` in a browser (no build step required).
3. For production, serve via static host (Netlify, GitHub Pages, etc.).
